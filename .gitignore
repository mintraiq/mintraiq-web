node_modules/
apps/ninja-react/node_modules/
mintraiq/react-embed/embed.html

# Generated browser public config (set PUBLIC_* or env.public, then npm run build:env)
config/runtime-env.js

# Local overrides (never commit secrets)
portal/js/config.local.js
.env
.env.local
# Local secrets — never commit (use config.example.json as a template)
config.json
# FastAPI mirror / local reference — canonical API lives in your backend repo; do not commit from this tree
finance_api.py
# Local Flask app reference (canonical app elsewhere)
expense_loader.py
# Local copy of env.public.example for build:env (tunnels, localhost)
env.public
